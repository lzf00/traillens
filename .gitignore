# 秘密
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / web (Sprint 4 落地)
node_modules/
.next/
.turbo/
dist/
build/

# 数据 / 模型 / 训练产物
data/
runs/

# 标注照片(不上传 GitHub;保留 .gitkeep + 说明)
packages/annotation/photos/*
!packages/annotation/photos/.gitkeep
!packages/annotation/photos/PUT_PHOTOS_HERE.md

# 剔除的非风光照(本地保留作 review;不上传 GitHub)
packages/annotation/photos_excluded/

# 预打分 + 标注数据(本地训练用,不上传 GitHub)
packages/annotation/data/*.jsonl
*.ckpt
*.safetensors
*.bin
*.pt
wandb/

# 操作系统 / 编辑器
.DS_Store
.idea/
.vscode/
*.swp

# Docker / 本地
*.log
postgres-data/
redis-data/
